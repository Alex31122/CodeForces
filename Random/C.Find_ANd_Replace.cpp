#include <iostream>
#include <string>

using namespace std;

int main(){

    //char s[2000];
    string s;
    cin >> s;
    s.find(s[0]);
    cout << s.find('o');
    cout << s[3];
    return 0;
}